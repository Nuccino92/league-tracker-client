/**
 *
 * @description
 * - this is a page for the entire player in the db
 * - it will hold the player image, name, current season team, each season stat history, current season game history, able to view other season game history
 */

export default function Player() {
  return <div>the playa</div>;
}
