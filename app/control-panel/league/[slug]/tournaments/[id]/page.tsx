import Container from '@/app/control-panel/_components/Container';

export default function TournamentPage() {
  //https://www.commoninja.com/brackets/editor/participants/

  /**
   * @todos
   *
   * 1. figure out the way the tournament tool should be, process etc.
   */
  return (
    <Container view='league'>
      <div className='h-[calc(100%-104px)] space-y-4'>tourny page</div>
    </Container>
  );
}
